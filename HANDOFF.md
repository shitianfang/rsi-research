# HANDOFF — 验证器形状这条线

写给下一个 session。**读这里不是为了拿结论，是为了知道去哪查结论、以及哪些结论已经被推翻。**
所有判断都会过期，`git log` 是真正的历史。

---

## 一句话

自改进系统的裁判都是**标量打分函数**。本线主张标量是错误的形状——它把"表现"和"尺子的松紧"编码进同一个数，
无法分解（moving-ruler 问题的代数形式：一个方程两个未知数）。**度量**输出位置 `z(A)` 和局部尺度 `M(A)`
两个可观测量，尺子变松时 `M` 整体膨胀、能力变好时 `z` 移动，两者分别可见。于是"谁验证验证器"这个递归被替换掉：
不证明裁判正确，只观测尺子有没有在动。

完整论证在 `plan.html`（也是给导师看的那份）。

**状态更新（2026-09-15 晚）：本线核心主张已按预注册作废。**四臂对照跑完，失败分支触发
（预算固定的臂涨得一样多，度量+上升自己平台化）；2605.22217 正文已读，其裁定与四臂负结果
指向同一处——要害在任务/意图生成器怎么持续变难（数据轴），不在裁判形状（奖励轴）。
幸存碎片：固定预算下度量显著优于标量（+0.059，2se 0.040）、E-A 各向异性 54.7×/22.3×。
细节全部在 `plan.html` 第 6、7 节。

**终局更新（同日深夜，需求对表后）：**主人指出 handoff 的方法本身可能就错，要求从需求出发。
对表结论（plan.html 第 9 节）：上面那句"核心主张作废"过宽——四臂杀死的是**引擎**主张
（难度内生），而需求（终端/Lean 级、无人、裁判自身演化时仍可信的反馈）的核心是**仪器**主张
（漂移可观测），它当时从未被测过。补做了 E-D / E-D2 两轮预注册实验（往裁判注入已知漂移、
往产物注入已知能力变化，看度量读数能否分辨而标量不能；漂移读数测在冻结参照件上——测标准件
不测产品）。结果：主对比「度量分出被刷裁判与真进步」在**两次注册跑里都显著**
（+0.616/2se 0.538；+1.152/2se 0.988），方向读数全部符合预期；但「标量不可分」的校准两次
都差一点（终局 MC 差 0.026——个别种子的真进步超出这种 hack 的分数天花板），按预注册终止条款
**最终裁定「不定」（final），本轮构造不再迭代**。引擎主张的作废不变。

## 东西在哪

| 路径 | 是什么 |
|---|---|
| `plan.html` | **主文档**。核心论点 + 七步方法 + 通用性 + 已测/待测 + 诚实的账 |
| `notes/directions/01-verifier-as-metric.md` | 原始方向笔记。**第九节记录了哪几句被自己的实验推翻**，原文保留未改 |
| `experiments/01-verifier-as-metric/sim.py` | 合成偏好世界，三个世界（含两个假设失效的）。已跑完，`run.log` 是输出 |
| `experiments/03-intent-recovery/intent.py` | 通用域（程序结构恢复）。E-0/E-A/E-B/E-C 全部跑完，`run.log` 是输出（含失败分支裁定） |
| `experiments/03-intent-recovery/drift.py` | E-D/E-D2：仪器主张（漂移可观测性）的两轮预注册实验，`drift-run.log` 是注册输出；判据与裁定在 plan.html 第 9 节 |
| `experiments/02-endogenous-metric-rsi/rsi.py` | **半废**。文件头标注了被证伪的构造和正确的重写方向 |
| `direction.html` / `papers.html` / `reports.html` | 2026-09-13 的阅读快照，未随本线更新 |

跑实验：`uv venv .venv && uv pip install --python .venv/bin/python numpy`，然后
`cd experiments/03-intent-recovery && ../../.venv/bin/python -u intent.py`。纯 numpy，无 GPU，全程分钟级。

## 已经被推翻的（不要重新主张）

- **几何自洽检验能替代真值锚 —— 错。** 六项检验里两项恒等空转（三角不等式对任何欧氏嵌入恒成立；
  循环率对标量验证器恒为 0，会给坏标量打满分）。其余三项与真实下游损害的秩相关只有 0.13–0.38，
  且恰好在"识别形状错误的验证器"这件事上失败。独立佐证：Double Ratchet（2607.12790）的锚消融——
  去掉锚，度量坍缩成"永远放行"，而**下游任务分数照样好看，所以发现不了**。
- **"标量有方向可被刷"不是新观点。** Transitivity Meets Cyclicity（2605.17342, ICML 2026）已做。
- **"无真值验证验证器"已有人做。** 2605.27914 的三张证书；该文还指出人类评分者自身相关仅 ρ≈0.45。
- **锚经济学 O(维度) 而非 O(样本) —— 未建立。** 运行间方差大于效应，4 种子仍未分离。
- **难度内生（预算阶梯）是复利引擎 —— 被自己的四臂推翻（2026-09-15，预注册失败分支）。**
  escalating vs fixed 在两种形状下都不显著（度量 −0.025，2se 0.048；标量 +0.028，2se 0.050），
  「度量+上升」自己平台化（Δ(60−40)=+0.005，2se 0.030）。幸存的次要发现：**固定预算下**度量
  显著优于标量（+0.059，2se 0.040）；E-A 各向异性 54.7×/22.3×；E-B 阶梯确实收敛到 50% 点。
  负结果边界：意图空间仅 6 种排列、产物 5 维，立即饱和——限制外推，但翻案只能靠新预注册实验。
- **「本线在调一个不绑定的变量」的风险已落地。** 2605.22217 正文确认（受控矩阵 7 配置 ×
  编码/DSL 双任务）：稳定性由**数据级有效性门控**单独绑定，无门时任何标量奖励变体都坍缩；
  它未测奖励形状、不涉漂移可观测性。稳定域内的绑定约束是 **proposer 容量天花板**（合格产出
  <1% 即停滞，靠课程/外部种子解决，不靠松门）——与我们四臂负结果同形。我们的构造里
  「意图先写」= 按构造自带有效性门（对应其 DSL 孪生「解释器即隐式门」论证），四臂全部稳定与此一致。

## 下一步（有先后）

handoff 原排的两步、需求对表补的 E-D/E-D2 都已完成（2026-09-15）。剩下的全是方向决策：

1. **仪器侧（需求核心）：是否开新一轮构造把「漂移可观测」立案。**两次注册的主信号都显著，
   卡在 hack 模型的分数天花板上。新一轮需要天花板永远可及的 hack（在**单次读取层面**插值
   诚实读取与先验猜测，而非轨迹层面的固定概率 p），作为全新预注册——这不是对本轮的翻案，
   终止条款只封本轮构造。
2. **引擎侧：「意图生成器持续变难」（任务准入轴）。**两份独立证据（2605.22217 的 proposer
   容量天花板 + 四臂的饱和形状）都指向这里。要求：意图空间可无界加深，判据先写死再跑。
   与仪器侧正交，可并行、可二选一、也可都不开。
3. `experiments/02` 的 MacAdam 重写只有在需要 M(A) 做真域漂移观测时才值得做，不要为救而救。
4. **不要重跑 E-C/E-D/E-D2 去找更好的数字。**各自的裁定都已按预注册执行；翻案只能靠新设计
   的新预注册实验，不能靠事后解释或调参。

## 踩过的坑（三次都差点从坏代码上读数）

这一节可能是本文件最有用的部分。**每次跑出"结果"之前，先证明被测对象真的在工作。**

1. **验证器根本没训练。** 梯度按总样本数归一化 → 每个参数步长≈0，留出准确率 0.50（纯随机），
   而实验照样输出了一整张看起来合理的结果表。修法：按参数计数归一化。
   `sim.py` 现在有 `assert_learned()`，准确率低于 0.55 会显式报警。
2. **匹配判断空转。** 两个随机产物几乎从不被"读成一样"（匹配率 0.01–0.05），拟合目标近似常数，
   于是 fit MSE 低得好看却毫无意义。教训：**先看目标变量的分布，再看拟合误差。**
3. **恢复器反向。** 用相邻两点差做跳变检测，噪声不随预算下降 → **预算越大恢复越差**。
   修法：窗口均值比较，标准误随预算收缩。**任何"预算/算力"轴上的实验，先验证单调性。**

另外：稻草人化基线会让结论作废。标量臂最初只有线性特征，而真值是理想点（二次），
**线性模型原理上就表示不了**——赢它证明不了任何事。现已给二次特征。搜索空间也必须有界，
否则线性目标跑到无穷远，那只说明"无界优化会发散"。

## 主人给的约束（会被检查）

- **领域通用。** 审美/设计是**动机域**不是允许的实现域。曾经交过一版海报专用的 harness，被"这只是设计层面，
  我要的是通用的 RSI"打回。任何提案的 oracle、度量、难度上升规则都必须用领域无关的语言陈述。
- **不要把冻结验证器的循环叫 RSI。** 人类数据一次性建好裁判然后冻结 = 只能爬到该裁判的最优点，
  和 autoresearch 撞顶是同一个失败。判据：信息量是否持续增长、改进者是否改进自己。
- **第一个实验域用程序不用设计**，因为那里有独立可计算的真值来核对度量有没有自欺。
- 反面结果照实写，被推翻的段落保留原处并标注，不要静默改掉。

## 未决

`plan.html` 已如实更新到终局（四臂失败分支、需求对表、E-D/E-D2 全过程与裁定都在；
第 1–5 节论证原样保留并在顶部标注）。悬而未决的只有方向决策：仪器侧新构造、引擎侧新主线、
两者都开、或全部归档——见上面下一步 1/2 条。这是主人的决定，等答复。
