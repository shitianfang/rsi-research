# HANDOFF — 验证器形状这条线

写给下一个 session。**读这里不是为了拿结论，是为了知道去哪查结论、以及哪些结论已经被推翻。**
所有判断都会过期，`git log` 是真正的历史。

---

## 尺子是什么（这一节被反复要求重讲，先讲清楚再谈别的）

**尺子 = 给裁判做的一张固定视力表。**

你不需要知道病人眼睛好不好。你只需要一张**永远不变**的表：上次能读到第 8 行，这次只能读到
第 3 行，那是眼睛变了，表没变。表里再混几行「两眼全遮」的题，用来抓瞎猜的。

翻译回这个项目：

- 表的每道题是**一对**东西（两张海报、两个回答、两个程序）。
- 题目只问一句：**「这两个一样吗？」**——从不问「这个好不好」。
- **正确答案我们天生就知道**，因为这些对子是我们**故意造成不同的**，另一些是故意造成**完全相同**的。
- 关键的分界线：**「这两个不一样」是免费的**（你自己弄的），**「这个东西好」才需要人或裁判**——
  而那正是我们没有、也不打算要的东西。所以这张表**不需要任何质量标注**。
- 读数 ＝（真不同的题里答「不同」的比例）**减去**（完全相同的题里答「不同」的比例）。
  后面那个减号就是「两眼全遮」对照，扣掉瞎猜。

**为什么它能反过来指证生产分数**：这张表从不参与生产。产物变好不会让表变简单，因为表上的题
一个像素都没动过。所以表上的读数一掉，**只可能是裁判坏了**。

海报演示里的真实数字（`demo-poster/`）：

| | 真不同的题答「不同」 | 完全相同的题答「不同」 | 读数 |
|---|---|---|---|
| 诚实裁判 | 1.00 | 0.04 | **0.96** |
| 被污染的裁判 | 0.35 | 0.35 | **0.03** |

单看 0.35 会以为污染裁判还有三成功力。**减掉瞎猜率才看出它根本没在看。**
不做这一步扣除，整场崩塌是看不见的——这是全线最容易被漏掉的一步。

---

## 状态板

### 已立案（合成域）
**仪器主张**：裁判漂移可观测。E-D3，12 个全新种子，判据跑前写死：

| 检验 | 结果 |
|---|---|
| 操纵检验 MC | +0.011（2se 0.027）不可分 ✔ 污染与真进步在分数上确实分不出 |
| 主判据 P1 | +0.503（2se 0.195）**显著** |
| 进步中仍可见 S1 | +0.475（2se 0.195）显著 |
| 变钝的尺子 S2 | +0.547（2se 0.089）显著 |
| 假警报 | −0.056 无 |

成立范围：合成程序域、单一 hack 家族、单一粗化方式、K_SEG=3。**到此为止，不要外推。**

### 已推翻（各自按预注册执行，不翻案）
- **引擎主张 · 难度调度版**（E-C 四臂）：预算上升臂不优于固定臂（度量 −0.025，2se 0.048；
  标量 +0.028，2se 0.050），且自己平台化。
- **引擎主张 · 课程必要性版**（E-E，`experiments/05-intent-escalation/`）：
  A 渐进 0.778 / C 直接上难题 0.803 / B 永远浅 0.674。
  **主判据 A−C = −0.025（2se 0.034）不显著 → 课程不必要**；次判据 A−B = +0.104（2se 0.065）显著。
  合起来一句话：**驱动持续改进的是任务供给，不是难度调度。**
- **几何自洽检验能替代真值锚**：六项里两项恒等空转，其余与真实损害秩相关仅 0.13–0.38。

### 悬着的
- **E-R 真实 LLM 裁判复现**（`experiments/04-llm-judge/`）：**2/6 块已完成并落盘，续跑不重跑**。
  块 2 结果很干净：污染让尺子读数 +0.250、真进步 0.000，分辨力 1.00→0.75。
  当时停下是主人喊停 + 宿主机被别的负载压到 load 300（那个 load 是**整台宿主机**的，不是本容器，
  别再误判成自己把机器压垮了）。
- **共同演化的出题者**：引擎主张最核心的一层，**从未被检验**。E-E 里的「任务生成器」只是个深度旋钮。

---

## 东西在哪

| 路径 | 是什么 |
|---|---|
| `demo-poster.html` | **给人看的演示**，海报从草稿到成品、被污染后烂掉、尺子当场抓住。页面由 `demo-poster/build_page.py` 生成，数字全部来自模拟 |
| `demo-poster/poster.py` | 海报模拟器。纯 numpy，4 秒跑完，7 项自检 |
| `briefing.html` | 方法简报：问题、机制、验证程度、诚实的账、局限 |
| `engine.html` | 引擎主张那一轮的单独交代（做了什么、为什么是负结果） |
| `plan.html` | **主文档**。全部论证 + 七步方法 + E-D/E-D2/E-D3/E-R 的完整预注册与裁定 |
| `experiments/03-intent-recovery/` | 玩具域：`intent.py`（E-0/A/B/C）、`drift.py`（E-D/E-D2/E-D3） |
| `experiments/04-llm-judge/` | 真实 LLM 裁判：`data.py` 建 JudgeBench 块、`er.py` 跑、`results/` 是已完成的块 |
| `experiments/05-intent-escalation/` | 引擎主张：`engine.py`，含四道仪器门 |
| `notes/directions/01-…md` | 原始方向笔记，第九节记录哪几句被自己的实验推翻 |

跑实验：`uv venv .venv && uv pip install --python .venv/bin/python numpy`。
E-R 需要 `claude` CLI 已登录（容器里没有 API key，裁判走 CLI）。

---

## 下一步（三个口子，互相正交）

1. **续跑 E-R 剩下 4 块** → 真实域复现的正式裁定。缓存让续跑免费。
   注意宿主机争抢时吞吐会从 0.8 秒/次掉到 12 秒/次，先探再跑。
2. **稀疏信号版引擎实验**。E-E 的负结果有一个明确边界：随机策略在最深层已有 0.705，**信号是稠密的**，
   而课程文献真正针对的是难题**完全给不出梯度**的情形。新一轮＝只用最深层全对/全错作为唯一反馈，
   并让出题者本身成为会进化的策略。**新的独立预注册，不是翻案。**
3. **仪器侧外部效度**：换第二个裁判模型、加第二个 hack 家族（位置偏置利用、冗长度偏好）。
   投稿前审稿人必问「单裁判单病变」。

---

## 踩过的坑（本文件最有用的一节）

**每次跑出「结果」之前，先证明被测对象真的在工作。** 这条纪律至今拦下五次错误结论：

1. **验证器根本没训练。** 梯度按总样本数归一化→每步≈0，留出准确率 0.50（纯随机），实验照样输出了
   一整张合理的结果表。修法：按参数计数归一化，并加 `assert_learned()`。
2. **匹配判断空转。** 两个随机产物几乎从不被读成一样（匹配率 0.01–0.05），拟合目标近似常数，
   MSE 低得好看却毫无意义。教训：**先看目标变量的分布，再看拟合误差。**
3. **恢复器反向。** 相邻两点差做跳变检测，噪声不随预算下降 → 预算越大恢复越差。
   **任何预算/算力轴上的实验，先验证单调性。**
4. **断点统计量用错**（E-E）：用「最大化左右均值差的绝对值」，该量在区间两端取极大——那里只有两三个
   样本、均值纯噪声，样本越多虚假极值越多。症状是**第 2 层保真度 0.38 低于随机**，且 64→256 预算不涨。
   修法：二分分割的标准统计量 `j(n−j)/n·(mL−mR)²`。
5. **爬山用陈旧分数**（海报演示）：当前方案的分数存下来不再更新，一个运气好的旧分会把后面所有真改进
   挡在门外。曲线因此抖得看不出趋势。修法：每轮两边都重新打分。

另外两条从这一轮新增：

- **稻草人化基线会让结论作废。** 标量臂最初只有线性特征而真值是二次的——赢它证明不了任何事。
- **不要从游走过程里挑某一轮当结论。** 裁判被污染后爬山退化成随机游走，任何单轮都只是一次取样。
  判据必须落在窗口统计上（我们用最后 20 轮）。海报演示里第 80 轮恰好走到高点，拿它当终点就是自欺。

---

## 主人给的约束（会被检查）

- **领域通用。** 审美/设计是**动机域**不是允许的实现域。曾经交过一版海报专用 harness，被
  「这只是设计层面，我要的是通用的 RSI」打回。`demo-poster/` 是**演示**，不是研究域——机制不得绑死在它上面。
- **不要把冻结验证器的循环叫 RSI。** 判据：信息量是否持续增长、改进者是否改进自己。
- **第一个实验域用程序不用设计**，因为那里有独立可计算的真值来核对度量有没有自欺。
- **反面结果照实写**，被推翻的段落保留原处并标注，不要静默改掉。
- **语料尽量用公开的**，不要自己生成。JudgeBench（MIT，客观正误）与 HelpSteer2（CC-BY-4.0，人工分级）
  都已实地核查可用；HelpSteer2 的主观分差撑不起「健康裁判」这道门（两个裁判在 gap=4 上与人工标注
  也只有约 50% 一致），这是实证结论，别再重蹈。
- **先想清楚需求，再执行继承来的方案。** 曾经忠实跑完一个预注册实验，事后才发现它测的是引擎主张，
  而需求的核心是仪器主张——测错了对象，还差点让它的失败裁定把整条线判死。

---

## 未决

方向决策留给主人：续跑 E-R、开稀疏信号引擎轮、补仪器外部效度、或就此归档。
`plan.html` 已如实更新到终局，`briefing.html` / `engine.html` / `demo-poster.html` 是三份给人看的交代。
